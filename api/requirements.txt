# EV211 field-api 런타임 의존성 — 재현 가능한 빌드를 위해 버전을 고정한다.
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.34.0
livekit-api==1.1.1
# 라이브 송신 비번 콜백(live_auth.py)이 직접 사용 — 3.14.1 취약점(PYSEC-2026-3545~47) 회피 하한.
aiohttp==3.14.3
livekit==1.1.13
pydantic==2.10.4
# 모드 C AI 통역 워커가 gpt-realtime-translate WebSocket 에 직결할 때 사용(직접 의존).
# livekit 이 이미 동일 버전을 끌어오므로 requirements.lock 과 정합한다.
websockets==16.1
